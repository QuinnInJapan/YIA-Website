diff --git a/node_modules/sanity-plugin-media/dist/index.mjs b/node_modules/sanity-plugin-media/dist/index.mjs
index 3ff7086..1b67142 100644
--- a/node_modules/sanity-plugin-media/dist/index.mjs
+++ b/node_modules/sanity-plugin-media/dist/index.mjs
@@ -182,7 +182,7 @@ const useKeyPress = (hotkey, onPress) => {
     field: "altText",
     name: "altText",
     operatorType: "empty",
-    operatorTypes: ["empty", "notEmpty", null, "includes", "doesNotInclude"],
+    operatorTypes: ["empty", "notEmpty", null, "を含む", "doesNotInclude"],
     title: "Alt text",
     type: "string",
     value: ""
@@ -192,7 +192,7 @@ const useKeyPress = (hotkey, onPress) => {
     field: "creditLine",
     name: "creditLine",
     operatorType: "empty",
-    operatorTypes: ["empty", "notEmpty", null, "includes", "doesNotInclude"],
+    operatorTypes: ["empty", "notEmpty", null, "を含む", "doesNotInclude"],
     title: "Credit",
     type: "string",
     value: ""
@@ -202,7 +202,7 @@ const useKeyPress = (hotkey, onPress) => {
     field: "description",
     name: "description",
     operatorType: "empty",
-    operatorTypes: ["empty", "notEmpty", null, "includes", "doesNotInclude"],
+    operatorTypes: ["empty", "notEmpty", null, "を含む", "doesNotInclude"],
     title: "Description",
     type: "string",
     value: ""
@@ -211,8 +211,8 @@ const useKeyPress = (hotkey, onPress) => {
     assetTypes: ["file", "image"],
     field: "originalFilename",
     name: "filename",
-    operatorType: "includes",
-    operatorTypes: ["includes", "doesNotInclude"],
+    operatorType: "を含む",
+    operatorTypes: ["を含む", "doesNotInclude"],
     title: "File name",
     type: "string",
     value: ""
@@ -237,37 +237,37 @@ const useKeyPress = (hotkey, onPress) => {
   inCurrentDocument: {
     assetTypes: ["file", "image"],
     name: "inCurrentDocument",
-    operatorType: "is",
+    operatorType: "である",
     options: [
       {
         name: "true",
-        title: "True",
+        title: "はい",
         value: groq`_id in $documentAssetIds`
       },
       {
         name: "false",
-        title: "False",
+        title: "いいえ",
         value: groq`!(_id in $documentAssetIds)`
       }
     ],
     selectOnly: !0,
-    title: "In use in current document",
+    title: "現在のドキュメントで使用中",
     type: "select",
     value: "true"
   },
   inUse: {
     assetTypes: ["file", "image"],
     name: "inUse",
-    operatorType: "is",
+    operatorType: "である",
     options: [
       {
         name: "true",
-        title: "True",
+        title: "はい",
         value: groq`count(*[references(^._id)]) > 0`
       },
       {
         name: "false",
-        title: "False",
+        title: "いいえ",
         value: groq`count(*[references(^._id)]) == 0`
       }
     ],
@@ -283,12 +283,12 @@ const useKeyPress = (hotkey, onPress) => {
     options: [
       {
         name: "true",
-        title: "True",
+        title: "はい",
         value: "false"
       },
       {
         name: "false",
-        title: "False",
+        title: "いいえ",
         value: "true"
       }
     ],
@@ -299,22 +299,22 @@ const useKeyPress = (hotkey, onPress) => {
   orientation: {
     assetTypes: ["image"],
     name: "orientation",
-    operatorType: "is",
-    operatorTypes: ["is", "isNot"],
+    operatorType: "である",
+    operatorTypes: ["である", "isNot"],
     options: [
       {
         name: "portrait",
-        title: "Portrait",
+        title: "縦向き",
         value: "metadata.dimensions.aspectRatio < 1"
       },
       {
         name: "landscape",
-        title: "Landscape",
+        title: "横向き",
         value: "metadata.dimensions.aspectRatio > 1"
       },
       {
         name: "square",
-        title: "Square",
+        title: "正方形",
         value: "metadata.dimensions.aspectRatio == 1"
       }
     ],
@@ -366,7 +366,7 @@ const useKeyPress = (hotkey, onPress) => {
     field: "title",
     name: "title",
     operatorType: "empty",
-    operatorTypes: ["empty", "notEmpty", null, "includes", "doesNotInclude"],
+    operatorTypes: ["empty", "notEmpty", null, "を含む", "doesNotInclude"],
     title: "Title",
     type: "string",
     value: ""
@@ -374,22 +374,22 @@ const useKeyPress = (hotkey, onPress) => {
   type: {
     assetTypes: ["file", "image"],
     name: "type",
-    operatorType: "is",
-    operatorTypes: ["is", "isNot"],
+    operatorType: "である",
+    operatorTypes: ["である", "isNot"],
     options: [
       {
         name: "image",
-        title: "Image",
+        title: "画像",
         value: 'mimeType match "image*"'
       },
       {
         name: "video",
-        title: "Video",
+        title: "動画",
         value: 'mimeType match "video*"'
       },
       {
         name: "audio",
-        title: "Audio",
+        title: "オーディオ",
         value: 'mimeType match "audio*"'
       },
       {
@@ -422,57 +422,57 @@ const useKeyPress = (hotkey, onPress) => {
 }, operators = {
   doesNotInclude: {
     fn: (value, field) => value ? `!(${field} match '*${value}*')` : void 0,
-    label: "does not include"
+    label: "を含まない"
   },
   doesNotReference: {
     fn: (value, _field) => value ? `!references('${value}')` : void 0,
-    label: "does not include"
+    label: "を含まない"
   },
   empty: {
     fn: (_value, field) => `!defined(${field})`,
     hideInput: !0,
-    label: "is empty"
+    label: "空である"
   },
   equalTo: {
     fn: (value, field) => value ? `${field} == ${value}` : void 0,
-    label: "is equal to"
+    label: "と等しい"
   },
   greaterThan: {
     fn: (value, field) => value ? `${field} > ${value}` : void 0,
-    label: "is greater than"
+    label: "より大きい"
   },
   greaterThanOrEqualTo: {
     fn: (value, field) => value ? `${field} >= ${value}` : void 0,
-    label: "is greater than or equal to"
+    label: "以上"
   },
   includes: {
     fn: (value, field) => value ? `${field} match '*${value}*'` : void 0,
-    label: "includes"
+    label: "を含む"
   },
   is: {
     fn: (value, _field) => `${value}`,
-    label: "is"
+    label: "である"
   },
   isNot: {
     fn: (value, _field) => `!(${value})`,
-    label: "is not"
+    label: "ではない"
   },
   lessThan: {
     fn: (value, field) => value ? `${field} < ${value}` : void 0,
-    label: "is less than"
+    label: "より小さい"
   },
   lessThanOrEqualTo: {
     fn: (value, field) => value ? `${field} <= ${value}` : void 0,
-    label: "is less than or equal to"
+    label: "以下"
   },
   notEmpty: {
     fn: (_value, field) => `defined(${field})`,
     hideInput: !0,
-    label: "is not empty"
+    label: "空でない"
   },
   references: {
     fn: (value, _field) => value ? `references('${value}')` : void 0,
-    label: "includes"
+    label: "を含む"
   }
 }, SUPPORTED_ASSET_TYPES = [
   "file",
@@ -551,24 +551,24 @@ const useKeyPress = (hotkey, onPress) => {
   return context;
 }, useVersionedClient = () => useClient({ apiVersion: "2022-10-01" }), ORDER_DICTIONARY = {
   _createdAt: {
-    asc: "Last created: Oldest first",
-    desc: "Last created: Newest first"
+    asc: "作成日：古い順",
+    desc: "作成日：新しい順"
   },
   _updatedAt: {
-    asc: "Last updated: Oldest first",
-    desc: "Last updated: Newest first"
+    asc: "更新日：古い順",
+    desc: "更新日：新しい順"
   },
   mimeType: {
-    asc: "MIME type: A to Z",
-    desc: "MIME type: Z to A"
+    asc: "MIMEタイプ：A→Z",
+    desc: "MIMEタイプ：Z→A"
   },
   originalFilename: {
-    asc: "File name: A to Z",
-    desc: "File name: Z to A"
+    asc: "ファイル名：A→Z",
+    desc: "ファイル名：Z→A"
   },
   size: {
-    asc: "File size: Smallest first",
-    desc: "File size: Largest first"
+    asc: "ファイルサイズ：小さい順",
+    desc: "ファイルサイズ：大きい順"
   }
 }, getOrderTitle = (field, direction) => ORDER_DICTIONARY[field][direction], debugThrottle = (throttled) => function(source) {
   return iif(
@@ -1705,7 +1705,7 @@ const customScrollbar = css`
         confirmText: `Yes, add tag to ${suffix}`,
         title: `Add tag ${tag.name.current} to ${suffix}?`,
         id: "confirm",
-        headerTitle: "Confirm tag addition",
+        headerTitle: "タグ追加の確認",
         tone: "primary",
         type: "confirm"
       });
@@ -1716,7 +1716,7 @@ const customScrollbar = css`
         closeDialogId,
         confirmCallbackAction: ASSETS_ACTIONS.tagsRemoveRequest({ assets: assetsPicked, tag }),
         confirmText: `Yes, remove tag from ${suffix}`,
-        headerTitle: "Confirm tag removal",
+        headerTitle: "タグ削除の確認",
         id: "confirm",
         title: `Remove tag ${tag.name.current} from ${suffix}?`,
         tone: "critical",
@@ -1731,10 +1731,10 @@ const customScrollbar = css`
           assets: assets.map((assetItem) => assetItem.asset)
         }),
         confirmText: `Yes, delete ${suffix}`,
-        description: "This operation cannot be reversed. Are you sure you want to continue?",
+        description: "この操作は元に戻せません。続行しますか？",
         title: `Permanently delete ${suffix}?`,
         id: "confirm",
-        headerTitle: "Confirm deletion",
+        headerTitle: "削除の確認",
         tone: "critical",
         type: "confirm"
       });
@@ -1745,10 +1745,10 @@ const customScrollbar = css`
         closeDialogId,
         confirmCallbackAction: tagsActions.deleteRequest({ tag }),
         confirmText: `Yes, delete ${suffix}`,
-        description: "This operation cannot be reversed. Are you sure you want to continue?",
+        description: "この操作は元に戻せません。続行しますか？",
         title: `Permanently delete ${suffix}?`,
         id: "confirm",
-        headerTitle: "Confirm deletion",
+        headerTitle: "削除の確認",
         tone: "critical",
         type: "confirm"
       });
@@ -2071,7 +2071,7 @@ const Container$1 = styled(Box)(({ $scheme, theme }) => css`
     dispatch(searchActions.facetsUpdate({ name: facet.name, value: option.name }));
   }, handleOperatorItemClick = (operatorType) => {
     dispatch(searchActions.facetsUpdate({ name: facet.name, operatorType }));
-  }, selectedOperatorType = facet?.operatorType ?? "is";
+  }, selectedOperatorType = facet?.operatorType ?? "である";
   return /* @__PURE__ */ jsxs(SearchFacet, { facet, children: [
     facet?.operatorTypes && /* @__PURE__ */ jsx(
       MenuButton,
@@ -2337,10 +2337,10 @@ const Container$1 = styled(Box)(({ $scheme, theme }) => css`
         isDisabled: tagsFetching,
         isSearchable: !0,
         name: "tags",
-        noOptionsMessage: () => "No tags",
+        noOptionsMessage: () => "タグなし",
         onChange: (value) => handleChange(value),
         options: allTagOptions,
-        placeholder: tagsFetching ? "Loading..." : "Select...",
+        placeholder: tagsFetching ? "読み込み中..." : "Select...",
         styles: reactSelectStyles$1(scheme),
         value: facet?.value
       }
@@ -2426,7 +2426,7 @@ const Container$1 = styled(Box)(({ $scheme, theme }) => css`
             icon: AddIcon,
             mode: "bleed",
             space: 2,
-            text: "Add filter",
+            text: "フィルターを追加",
             tone: "primary"
           }
         ),
@@ -2444,7 +2444,7 @@ const Container$1 = styled(Box)(({ $scheme, theme }) => css`
         fontSize: 1,
         mode: "bleed",
         onClick: () => dispatch(searchActions.facetsClear()),
-        text: "Clear"
+        text: "クリア"
       }
     )
   ] });
@@ -2485,7 +2485,7 @@ const Container$1 = styled(Box)(({ $scheme, theme }) => css`
         fontSize: 1,
         icon: SearchIcon,
         onChange: handleChange,
-        placeholder: "Search",
+        placeholder: "検索",
         radius: 2,
         value: searchQuery
       }
@@ -2659,8 +2659,8 @@ const DebugControls = () => {
     }
   ) : null;
 }, tagOptionSchema = z.object({
-  label: z.string().trim().min(1, { message: "Label cannot be empty" }),
-  value: z.string().trim().min(1, { message: "Value cannot be empty" })
+  label: z.string().trim().min(1, { message: "ラベルを入力してください" }),
+  value: z.string().trim().min(1, { message: "値を入力してください" })
 }), assetFormSchema = z.object({
   altText: z.string().trim().optional(),
   creditLine: z.string().trim().optional(),
@@ -2670,10 +2670,10 @@ const DebugControls = () => {
       tags: z.array(tagOptionSchema).nullable()
     })
   }),
-  originalFilename: z.string().trim().min(1, { message: "Filename cannot be empty" }),
+  originalFilename: z.string().trim().min(1, { message: "ファイル名を入力してください" }),
   title: z.string().trim().optional()
 }), tagFormSchema = z.object({
-  name: z.string().min(1, { message: "Name cannot be empty" })
+  name: z.string().min(1, { message: "名前を入力してください" })
 });
 function getUniqueDocuments(documents) {
   const draftIds = documents.reduce(
@@ -2701,7 +2701,7 @@ const imageDprUrl = (asset, options) => {
   }, []), /* @__PURE__ */ jsx(
     Popover,
     {
-      content: /* @__PURE__ */ jsx(Text, { muted: !0, size: 1, children: "Copied!" }),
+      content: /* @__PURE__ */ jsx(Text, { muted: !0, size: 1, children: "コピーしました" }),
       open: popoverVisible,
       padding: 2,
       placement: "top",
@@ -2715,7 +2715,7 @@ const imageDprUrl = (asset, options) => {
           icon: ClipboardIcon,
           mode: "ghost",
           onClick: handleClick,
-          text: "Copy URL"
+          text: "URLをコピー"
         }
       )
     }
@@ -2752,10 +2752,10 @@ const imageDprUrl = (asset, options) => {
   };
   return /* @__PURE__ */ jsxs(Box, { marginTop: 3, children: [
     /* @__PURE__ */ jsx(Box, { children: /* @__PURE__ */ jsxs(Stack, { space: 3, children: [
-      /* @__PURE__ */ jsx(Row, { label: "Size", value: filesize(asset?.size, { base: 10, round: 0 }) }),
-      /* @__PURE__ */ jsx(Row, { label: "MIME type", value: asset?.mimeType }),
-      /* @__PURE__ */ jsx(Row, { label: "Extension", value: (asset?.extension).toUpperCase() }),
-      isImageAsset(asset) && /* @__PURE__ */ jsx(Row, { label: "Dimensions", value: getAssetResolution(asset) })
+      /* @__PURE__ */ jsx(Row, { label: "サイズ", value: filesize(asset?.size, { base: 10, round: 0 }) }),
+      /* @__PURE__ */ jsx(Row, { label: "MIMEタイプ", value: asset?.mimeType }),
+      /* @__PURE__ */ jsx(Row, { label: "拡張子", value: (asset?.extension).toUpperCase() }),
+      isImageAsset(asset) && /* @__PURE__ */ jsx(Row, { label: "解像度", value: getAssetResolution(asset) })
     ] }) }),
     exif && (exif.DateTimeOriginal || exif.FNumber || exif.FocalLength || exif.ExposureTime || exif.ISO) && /* @__PURE__ */ jsxs(Fragment, { children: [
       /* @__PURE__ */ jsx(
@@ -2771,13 +2771,13 @@ const imageDprUrl = (asset, options) => {
       ),
       /* @__PURE__ */ jsx(Box, { children: /* @__PURE__ */ jsxs(Stack, { space: 3, children: [
         exif.ISO && /* @__PURE__ */ jsx(Row, { label: "ISO", value: exif.ISO }),
-        exif.FNumber && /* @__PURE__ */ jsx(Row, { label: "Aperture", value: `\u0192/${exif.FNumber}` }),
-        exif.FocalLength && /* @__PURE__ */ jsx(Row, { label: "Focal length", value: `${exif.FocalLength}mm` }),
-        exif.ExposureTime && /* @__PURE__ */ jsx(Row, { label: "Exposure time", value: `1/${1 / exif.ExposureTime}` }),
+        exif.FNumber && /* @__PURE__ */ jsx(Row, { label: "絞り", value: `\u0192/${exif.FNumber}` }),
+        exif.FocalLength && /* @__PURE__ */ jsx(Row, { label: "焦点距離", value: `${exif.FocalLength}mm` }),
+        exif.ExposureTime && /* @__PURE__ */ jsx(Row, { label: "露出時間", value: `1/${1 / exif.ExposureTime}` }),
         exif.DateTimeOriginal && /* @__PURE__ */ jsx(
           Row,
           {
-            label: "Original date",
+            label: "撮影日",
             value: format(new Date(exif.DateTimeOriginal), "PPp")
           }
         )
@@ -2792,7 +2792,7 @@ const imageDprUrl = (asset, options) => {
           icon: DownloadIcon,
           mode: "ghost",
           onClick: handleDownload,
-          text: "Download"
+          text: "ダウンロード"
         }
       ),
       /* @__PURE__ */ jsx(ButtonAssetCopy, { disabled: !item || item?.updating, url: asset.url })
@@ -2800,7 +2800,7 @@ const imageDprUrl = (asset, options) => {
   ] });
 }, Dialog = (props) => /* @__PURE__ */ jsx(Dialog$1, { ...props, style: { position: "fixed" } }), DocumentList = ({ documents, isLoading }) => {
   const schema = useSchema();
-  return isLoading ? /* @__PURE__ */ jsx(Text, { muted: !0, size: 1, children: "Loading..." }) : documents.length === 0 ? /* @__PURE__ */ jsx(Text, { muted: !0, size: 1, children: "No documents are referencing this asset" }) : /* @__PURE__ */ jsx(Card, { flex: 1, marginBottom: 2, padding: 2, radius: 2, shadow: 1, children: /* @__PURE__ */ jsx(Stack, { space: 2, children: documents?.map((doc) => /* @__PURE__ */ jsx(ReferringDocument, { doc, schemaType: schema.get(doc._type) }, doc._id)) }) });
+  return isLoading ? /* @__PURE__ */ jsx(Text, { muted: !0, size: 1, children: "読み込み中..." }) : documents.length === 0 ? /* @__PURE__ */ jsx(Text, { muted: !0, size: 1, children: "このアセットを参照しているドキュメントはありません" }) : /* @__PURE__ */ jsx(Card, { flex: 1, marginBottom: 2, padding: 2, radius: 2, shadow: 1, children: /* @__PURE__ */ jsx(Stack, { space: 2, children: documents?.map((doc) => /* @__PURE__ */ jsx(ReferringDocument, { doc, schemaType: schema.get(doc._type) }, doc._id)) }) });
 }, ReferringDocument = (props) => {
   const { doc, schemaType } = props, { onClick } = useIntentLink({
     intent: "edit",
@@ -2846,7 +2846,7 @@ const imageDprUrl = (asset, options) => {
     /* @__PURE__ */ jsx("br", {}),
     " ",
     format(new Date(lastUpdated), "PPp")
-  ] }) : content = "No unpublished changes" : content = "There are validation errors that need to be fixed before this document can be published", /* @__PURE__ */ jsx(
+  ] }) : content = "未公開の変更はありません" : content = "There are validation errors that need to be fixed before this document can be published", /* @__PURE__ */ jsx(
     Tooltip,
     {
       animate: !0,
@@ -2860,7 +2860,7 @@ const imageDprUrl = (asset, options) => {
           disabled,
           fontSize: 1,
           onClick,
-          text: "Save and close",
+          text: "保存して閉じる",
           tone: "primary"
         }
       ) })
@@ -3052,12 +3052,12 @@ const imageDprUrl = (asset, options) => {
                   isLoading: creating,
                   isMulti: !0,
                   name,
-                  noOptionsMessage: () => "No tags",
+                  noOptionsMessage: () => "タグなし",
                   onBlur,
                   onChange,
                   onCreateOption: onCreateTag,
                   options,
-                  placeholder: tagsFetching ? "Loading..." : placeholder,
+                  placeholder: tagsFetching ? "読み込み中..." : placeholder,
                   styles: reactSelectStyles(scheme),
                   value: controllerValue
                 }
@@ -3125,11 +3125,11 @@ function Details({
         control,
         disabled: formUpdating,
         error: errors?.opt?.media?.tags?.message,
-        label: "Tags",
+        label: "タグ",
         name: "opt.media.tags",
         onCreateTag: handleCreateTag,
         options: allTagOptions,
-        placeholder: "Select or create...",
+        placeholder: "選択または作成...",
         value: assetTagOptions
       }
     ),
@@ -3139,7 +3139,7 @@ function Details({
         ...register("originalFilename"),
         disabled: formUpdating,
         error: errors?.originalFilename?.message,
-        label: "Filename",
+        label: "ファイル名",
         name: "originalFilename",
         value: currentAsset?.originalFilename
       }
@@ -3150,7 +3150,7 @@ function Details({
         ...register("title"),
         disabled: formUpdating,
         error: errors?.title?.message,
-        label: "Title",
+        label: "タイトル",
         name: "title",
         value: currentAsset?.title
       }
@@ -3161,7 +3161,7 @@ function Details({
         ...register("altText"),
         disabled: formUpdating,
         error: errors?.altText?.message,
-        label: "Alt Text",
+        label: "代替テキスト",
         name: "altText",
         value: currentAsset?.altText
       }
@@ -3172,7 +3172,7 @@ function Details({
         ...register("description"),
         disabled: formUpdating,
         error: errors?.description?.message,
-        label: "Description",
+        label: "説明",
         name: "description",
         rows: 5,
         value: currentAsset?.description
@@ -3183,7 +3183,7 @@ function Details({
       {
         ...register("creditLine"),
         error: errors?.creditLine?.message,
-        label: "Credit",
+        label: "クレジット",
         name: "creditLine",
         value: currentAsset?.creditLine,
         disabled: formUpdating || creditLine?.excludeSources?.includes(currentAsset?.source?.name)
@@ -3299,7 +3299,7 @@ const DialogAssetEdit = (props) => {
         fontSize: 1,
         mode: "bleed",
         onClick: handleDelete,
-        text: "Delete",
+        text: "削除",
         tone: "critical"
       }
     ),
@@ -3332,7 +3332,7 @@ const DialogAssetEdit = (props) => {
     {
       animate: !0,
       footer: /* @__PURE__ */ jsx(Footer, {}),
-      header: "Asset details",
+      header: "アセットの詳細",
       id,
       onClose: handleClose,
       width: 3,
@@ -3348,7 +3348,7 @@ const DialogAssetEdit = (props) => {
                     "aria-controls": "details-panel",
                     disabled: formUpdating,
                     id: "details-tab",
-                    label: "Details",
+                    label: "詳細",
                     onClick: () => setTabSection("details"),
                     selected: tabSection === "details",
                     size: 2
@@ -3434,7 +3434,7 @@ const DialogAssetEdit = (props) => {
     {
       animate: !0,
       footer: /* @__PURE__ */ jsx(() => /* @__PURE__ */ jsx(Box, { padding: 3, children: /* @__PURE__ */ jsxs(Flex, { justify: "space-between", children: [
-        /* @__PURE__ */ jsx(Button, { fontSize: 1, mode: "bleed", onClick: handleClose, text: "Cancel" }),
+        /* @__PURE__ */ jsx(Button, { fontSize: 1, mode: "bleed", onClick: handleClose, text: "キャンセル" }),
         /* @__PURE__ */ jsx(
           Button,
           {
@@ -3468,7 +3468,7 @@ const DialogAssetEdit = (props) => {
   } = props, dispatch = useDispatch(), handleClose = useCallback(() => {
     dispatch(dialogActions.clear());
   }, []);
-  return /* @__PURE__ */ jsxs(Dialog, { animate: !0, header: "Filters", id, onClose: handleClose, width: 1, children: [
+  return /* @__PURE__ */ jsxs(Dialog, { animate: !0, header: "フィルター", id, onClose: handleClose, width: 1, children: [
     /* @__PURE__ */ jsxs(Box, { padding: 3, children: [
       /* @__PURE__ */ jsx(SearchFacets, { layout: "stack" }),
       /* @__PURE__ */ jsx(SearchFacetsControl, {})
@@ -3508,7 +3508,7 @@ const DialogAssetEdit = (props) => {
       isValid,
       onClick: handleSubmit(onSubmit)
     }
-  ) }) }), {}), header: "Create Tag", id, onClose: handleClose, width: 1, children: [
+  ) }) }), {}), header: "タグを作成", id, onClose: handleClose, width: 1, children: [
     /* @__PURE__ */ jsxs(Box, { as: "form", padding: 4, onSubmit: handleSubmit(onSubmit), children: [
       /* @__PURE__ */ jsx("button", { style: { display: "none" }, tabIndex: -1, type: "submit" }),
       /* @__PURE__ */ jsx(
@@ -3517,7 +3517,7 @@ const DialogAssetEdit = (props) => {
           ...register("name"),
           disabled: formUpdating,
           error: errors?.name?.message,
-          label: "Name",
+          label: "名前",
           name: "name"
         }
       )
@@ -3592,7 +3592,7 @@ const DialogAssetEdit = (props) => {
         fontSize: 1,
         mode: "bleed",
         onClick: handleDelete,
-        text: "Delete",
+        text: "削除",
         tone: "critical"
       }
     ),
@@ -3605,7 +3605,7 @@ const DialogAssetEdit = (props) => {
         onClick: handleSubmit(onSubmit)
       }
     )
-  ] }) }), {}), header: "Edit Tag", id, onClose: handleClose, width: 1, children: [
+  ] }) }), {}), header: "タグを編集", id, onClose: handleClose, width: 1, children: [
     /* @__PURE__ */ jsxs(Box, { as: "form", padding: 4, onSubmit: handleSubmit(onSubmit), children: [
       !tagItem && /* @__PURE__ */ jsx(Card, { marginBottom: 3, padding: 3, radius: 2, shadow: 1, tone: "critical", children: /* @__PURE__ */ jsx(Text, { size: 1, children: "This tag cannot be found \u2013 it may have been deleted." }) }),
       /* @__PURE__ */ jsx("button", { style: { display: "none" }, tabIndex: -1, type: "submit" }),
@@ -3615,7 +3615,7 @@ const DialogAssetEdit = (props) => {
           ...register("name"),
           disabled: formUpdating,
           error: errors?.name?.message,
-          label: "Name",
+          label: "名前",
           name: "name"
         }
       )
@@ -3838,7 +3838,7 @@ const DialogAssetEdit = (props) => {
       children: [
         /* @__PURE__ */ jsxs(Inline, { space: 2, children: [
           /* @__PURE__ */ jsx(Label, { size: 0, children: title }),
-          tagsFetching && /* @__PURE__ */ jsx(Label, { size: 0, style: { opacity: 0.3 }, children: "Loading..." })
+          tagsFetching && /* @__PURE__ */ jsx(Label, { size: 0, style: { opacity: 0.3 }, children: "読み込み中..." })
         ] }),
         allowCreate && /* @__PURE__ */ jsx(Box, { marginRight: 1, children: /* @__PURE__ */ jsx(
           Button,
@@ -3868,7 +3868,7 @@ const DialogAssetEdit = (props) => {
         title: hasPicked ? "Tags (in selection)" : "Tags"
       }
     ),
-    fetchComplete && !hasTags && /* @__PURE__ */ jsx(Box, { padding: 3, children: /* @__PURE__ */ jsx(Text, { muted: !0, size: 1, children: /* @__PURE__ */ jsx("em", { children: "No tags" }) }) }),
+    fetchComplete && !hasTags && /* @__PURE__ */ jsx(Box, { padding: 3, children: /* @__PURE__ */ jsx(Text, { muted: !0, size: 1, children: /* @__PURE__ */ jsx("em", { children: "タグなし" }) }) }),
     hasTags && /* @__PURE__ */ jsx(TagsVirtualized, {})
   ] });
 }, DialogTags = (props) => {
@@ -3878,7 +3878,7 @@ const DialogAssetEdit = (props) => {
   } = props, dispatch = useDispatch(), handleClose = useCallback(() => {
     dispatch(dialogActions.clear());
   }, []);
-  return /* @__PURE__ */ jsxs(Dialog, { animate: !0, header: "All Tags", id, onClose: handleClose, width: 1, children: [
+  return /* @__PURE__ */ jsxs(Dialog, { animate: !0, header: "すべてのタグ", id, onClose: handleClose, width: 1, children: [
     /* @__PURE__ */ jsx(
       Box,
       {
@@ -3912,7 +3912,7 @@ const DialogAssetEdit = (props) => {
   const { onClose } = props, { open } = useDropzoneActions(), { onSelect } = useAssetSourceActions(), assetTypes = useTypedSelector((state) => state.assets.assetTypes), selectedDocument = useTypedSelector((state) => state.selected.document), { directUploads } = useToolOptions();
   return /* @__PURE__ */ jsx(Box, { paddingY: 2, children: /* @__PURE__ */ jsxs(Flex, { align: "center", justify: "space-between", children: [
     /* @__PURE__ */ jsx(Box, { flex: 1, marginX: 3, children: /* @__PURE__ */ jsxs(Inline, { style: { whiteSpace: "nowrap" }, children: [
-      /* @__PURE__ */ jsx(Text, { textOverflow: "ellipsis", weight: "semibold", children: /* @__PURE__ */ jsx("span", { children: onSelect ? `Insert ${assetTypes.join(" or ")}` : "Browse Assets" }) }),
+      /* @__PURE__ */ jsx(Text, { textOverflow: "ellipsis", weight: "semibold", children: /* @__PURE__ */ jsx("span", { children: onSelect ? `Insert ${assetTypes.join(" or ")}` : "アセットを閲覧" }) }),
       selectedDocument && /* @__PURE__ */ jsx(Box, { display: ["none", "none", "block"], children: /* @__PURE__ */ jsxs(Text, { children: [
         /* @__PURE__ */ jsx("span", { style: { margin: "0 0.5em" }, children: /* @__PURE__ */ jsx(Icon, { symbol: "arrow-right" }) }),
         /* @__PURE__ */ jsx("span", { style: { textTransform: "capitalize" }, children: selectedDocument._type })
@@ -4409,7 +4409,7 @@ const CardWrapper = styled(Flex)`
     return null;
   const fileSize = filesize(item.size, { base: 10, round: 0 }), percentLoaded = Math.round(item.percent || 0), isComplete = item.status === "complete", isUploading = item.status === "uploading", isQueued = item.status === "queued";
   let status;
-  isComplete && (status = "Verifying"), isUploading && (status = `${percentLoaded}%`), isQueued && (status = "Queued");
+  isComplete && (status = "確認中"), isUploading && (status = `${percentLoaded}%`), isQueued && (status = "待機中");
   const handleCancelUpload = () => {
     dispatch(uploadsActions.uploadCancel({ hash: item.hash }));
   };
@@ -4638,7 +4638,7 @@ const CardWrapper = styled(Flex)`
         /* @__PURE__ */ jsx(TableHeaderItem, { field: "mimeType", title: "MIME type" }),
         /* @__PURE__ */ jsx(TableHeaderItem, { field: "size", title: "Size" }),
         /* @__PURE__ */ jsx(TableHeaderItem, { field: "_updatedAt", title: "Last updated" }),
-        /* @__PURE__ */ jsx(TableHeaderItem, { title: "References" }),
+        /* @__PURE__ */ jsx(TableHeaderItem, { title: "参照" }),
         /* @__PURE__ */ jsx(TableHeaderItem, {})
       ]
     }
@@ -4902,7 +4902,7 @@ const TableRowUpload = (props) => {
     return null;
   const fileSize = filesize(item.size, { base: 10, round: 0 }), percentLoaded = Math.round(item.percent || 0), isComplete = item.status === "complete", isUploading = item.status === "uploading", isQueued = item.status === "queued";
   let status;
-  isComplete && (status = "Verifying"), isUploading && (status = `${percentLoaded}%`), isQueued && (status = "Queued");
+  isComplete && (status = "確認中"), isUploading && (status = `${percentLoaded}%`), isQueued && (status = "待機中");
   const handleCancelUpload = () => {
     dispatch(uploadsActions.uploadCancel({ hash: item.hash }));
   };
@@ -5082,7 +5082,7 @@ const TableRowUpload = (props) => {
             padding: 2,
             style: { background: "none", boxShadow: "none" },
             tone: "default",
-            children: /* @__PURE__ */ jsx(Label, { size: 0, children: "Deselect" })
+            children: /* @__PURE__ */ jsx(Label, { size: 0, children: "選択解除" })
           }
         ),
         /* @__PURE__ */ jsx(
@@ -5093,7 +5093,7 @@ const TableRowUpload = (props) => {
             padding: 2,
             style: { background: "none", boxShadow: "none" },
             tone: "critical",
-            children: /* @__PURE__ */ jsx(Label, { size: 0, children: "Delete" })
+            children: /* @__PURE__ */ jsx(Label, { size: 0, children: "削除" })
           }
         )
       ] })
@@ -5205,13 +5205,13 @@ const initialState = {
   })
 ), notificationsTagCreateCompleteEpic = (action$) => action$.pipe(
   filter(tagsActions.createComplete.match),
-  mergeMap(() => of(notificationsSlice.actions.add({ status: "info", title: "Tag created" })))
+  mergeMap(() => of(notificationsSlice.actions.add({ status: "info", title: "タグを作成しました" })))
 ), notificationsTagDeleteCompleteEpic = (action$) => action$.pipe(
   filter(tagsActions.deleteComplete.match),
-  mergeMap(() => of(notificationsSlice.actions.add({ status: "info", title: "Tag deleted" })))
+  mergeMap(() => of(notificationsSlice.actions.add({ status: "info", title: "タグを削除しました" })))
 ), notificationsTagUpdateCompleteEpic = (action$) => action$.pipe(
   filter(tagsActions.updateComplete.match),
-  mergeMap(() => of(notificationsSlice.actions.add({ status: "info", title: "Tag updated" })))
+  mergeMap(() => of(notificationsSlice.actions.add({ status: "info", title: "タグを更新しました" })))
 ), notificationsActions = { ...notificationsSlice.actions };
 var notificationsReducer = notificationsSlice.reducer;
 const rootEpic = combineEpics(
@@ -5412,7 +5412,7 @@ const UploadDropzone = (props) => {
     rejections.flatMap(({ errors }) => errors.map(({ code }) => code)).includes("file-too-large") && dispatch(
       notificationsActions.add({
         status: "error",
-        title: "One or more files exceed the maximum upload size."
+        title: "アップロードサイズの上限を超えるファイルがあります。"
       })
     );
   }, handleFileGetter = async (event) => {
@@ -5444,7 +5444,7 @@ const UploadDropzone = (props) => {
   });
   return /* @__PURE__ */ jsx(DropzoneDispatchProvider, { open, children: /* @__PURE__ */ jsxs(UploadContainer, { ...getRootProps(), children: [
     /* @__PURE__ */ jsx("input", { ...getInputProps() }),
-    isDragActive && /* @__PURE__ */ jsx(DragActiveContainer, { children: /* @__PURE__ */ jsx(Flex, { direction: "column", justify: "center", style: { color: white.hex }, children: /* @__PURE__ */ jsx(Text, { size: 3, style: { color: "inherit" }, children: "Drop files to upload" }) }) }),
+    isDragActive && /* @__PURE__ */ jsx(DragActiveContainer, { children: /* @__PURE__ */ jsx(Flex, { direction: "column", justify: "center", style: { color: white.hex }, children: /* @__PURE__ */ jsx(Text, { size: 3, style: { color: "inherit" }, children: "ファイルをドロップしてアップロード" }) }) }),
     children
   ] }) });
 }, BrowserContent = ({ onClose }) => {
@@ -5528,7 +5528,7 @@ const UploadDropzone = (props) => {
   }), [container]), container;
 }, Tool = () => /* @__PURE__ */ jsx(Flex, { direction: "column", height: "fill", flex: 1, children: /* @__PURE__ */ jsx(Browser, {}) });
 var mediaTag = {
-  title: "Media Tag",
+  title: "メディアタグ",
   icon: TagIcon,
   name: TAG_DOCUMENT_NAME,
   type: "document",
@@ -5555,7 +5555,7 @@ var mediaTag = {
 const plugin = {
   icon: ImageIcon,
   name: "media",
-  title: "Media"
+  title: "メディア"
 }, mediaAssetSource = {
   ...plugin,
   component: FormBuilderTool
diff --git a/node_modules/sanity-plugin-media/src/components/AssetMetadata/index.tsx b/node_modules/sanity-plugin-media/src/components/AssetMetadata/index.tsx
index a3f3e0e..30d65b2 100644
--- a/node_modules/sanity-plugin-media/src/components/AssetMetadata/index.tsx
+++ b/node_modules/sanity-plugin-media/src/components/AssetMetadata/index.tsx
@@ -56,10 +56,10 @@ const AssetMetadata = (props: Props) => {
       {/* Base */}
       <Box>
         <Stack space={3}>
-          <Row label="Size" value={filesize(asset?.size, {base: 10, round: 0})} />
-          <Row label="MIME type" value={asset?.mimeType} />
-          <Row label="Extension" value={(asset?.extension).toUpperCase()} />
-          {isImageAsset(asset) && <Row label="Dimensions" value={getAssetResolution(asset)} />}
+          <Row label="サイズ" value={filesize(asset?.size, {base: 10, round: 0})} />
+          <Row label="MIMEタイプ" value={asset?.mimeType} />
+          <Row label="拡張子" value={(asset?.extension).toUpperCase()} />
+          {isImageAsset(asset) && <Row label="解像度" value={getAssetResolution(asset)} />}
         </Stack>
       </Box>
       {/* EXIF */}
@@ -82,14 +82,14 @@ const AssetMetadata = (props: Props) => {
             <Box>
               <Stack space={3}>
                 {exif.ISO && <Row label="ISO" value={exif.ISO} />}
-                {exif.FNumber && <Row label="Aperture" value={`ƒ/${exif.FNumber}`} />}
-                {exif.FocalLength && <Row label="Focal length" value={`${exif.FocalLength}mm`} />}
+                {exif.FNumber && <Row label="絞り" value={`ƒ/${exif.FNumber}`} />}
+                {exif.FocalLength && <Row label="焦点距離" value={`${exif.FocalLength}mm`} />}
                 {exif.ExposureTime && (
-                  <Row label="Exposure time" value={`1/${1 / exif.ExposureTime}`} />
+                  <Row label="露出時間" value={`1/${1 / exif.ExposureTime}`} />
                 )}
                 {exif.DateTimeOriginal && (
                   <Row
-                    label="Original date"
+                    label="撮影日"
                     value={format(new Date(exif.DateTimeOriginal), 'PPp')}
                   />
                 )}
@@ -108,7 +108,7 @@ const AssetMetadata = (props: Props) => {
             icon={DownloadIcon}
             mode="ghost"
             onClick={handleDownload}
-            text="Download"
+            text="ダウンロード"
           />
           {/* Copy to clipboard */}
           <ButtonAssetCopy disabled={!item || item?.updating} url={asset.url} />
diff --git a/node_modules/sanity-plugin-media/src/components/ButtonAssetCopy/index.tsx b/node_modules/sanity-plugin-media/src/components/ButtonAssetCopy/index.tsx
index e3e2efb..0cfd6b3 100644
--- a/node_modules/sanity-plugin-media/src/components/ButtonAssetCopy/index.tsx
+++ b/node_modules/sanity-plugin-media/src/components/ButtonAssetCopy/index.tsx
@@ -40,7 +40,7 @@ const ButtonAssetCopy = ({disabled, url}: Props) => {
     <Popover
       content={
         <Text muted size={1}>
-          Copied!
+          コピーしました
         </Text>
       }
       open={popoverVisible}
@@ -55,7 +55,7 @@ const ButtonAssetCopy = ({disabled, url}: Props) => {
         icon={ClipboardIcon}
         mode="ghost"
         onClick={handleClick}
-        text="Copy URL"
+        text="URLをコピー"
       />
     </Popover>
   )
diff --git a/node_modules/sanity-plugin-media/src/components/CardUpload/index.tsx b/node_modules/sanity-plugin-media/src/components/CardUpload/index.tsx
index 0b79a58..648794b 100644
--- a/node_modules/sanity-plugin-media/src/components/CardUpload/index.tsx
+++ b/node_modules/sanity-plugin-media/src/components/CardUpload/index.tsx
@@ -45,13 +45,13 @@ const CardUpload = (props: Props) => {
 
   let status
   if (isComplete) {
-    status = 'Verifying'
+    status = '確認中'
   }
   if (isUploading) {
     status = `${percentLoaded}%`
   }
   if (isQueued) {
-    status = 'Queued'
+    status = '待機中'
   }
 
   // Callbacks
diff --git a/node_modules/sanity-plugin-media/src/components/Controls/index.tsx b/node_modules/sanity-plugin-media/src/components/Controls/index.tsx
index d8eabef..40353e9 100644
--- a/node_modules/sanity-plugin-media/src/components/Controls/index.tsx
+++ b/node_modules/sanity-plugin-media/src/components/Controls/index.tsx
@@ -80,7 +80,7 @@ const Controls = () => {
                   fontSize={1}
                   mode="ghost"
                   onClick={handleShowSearchFacetDialog}
-                  text={`Filters${searchFacets.length > 0 ? ` (${searchFacets.length})` : ''}`}
+                  text={`フィルター${searchFacets.length > 0 ? `（${searchFacets.length}）` : ''}`}
                   tone="primary"
                 />
 
@@ -89,7 +89,7 @@ const Controls = () => {
                   fontSize={1}
                   mode="ghost"
                   onClick={handleShowTagsDialog}
-                  text={`Tags`}
+                  text={`タグ`}
                   tone="primary"
                 />
               </Inline>
@@ -119,7 +119,7 @@ const Controls = () => {
                 }
                 onClick={toggleTagsPanelToggle}
                 mode={tagsPanelVisible ? 'default' : 'ghost'}
-                text={tagsPanelVisible ? 'Tags' : ''}
+                text={tagsPanelVisible ? 'タグ' : ''}
               />
             </Box>
           </Flex>
diff --git a/node_modules/sanity-plugin-media/src/components/DialogAssetEdit/Details.tsx b/node_modules/sanity-plugin-media/src/components/DialogAssetEdit/Details.tsx
index d4d3d01..8c81dcd 100644
--- a/node_modules/sanity-plugin-media/src/components/DialogAssetEdit/Details.tsx
+++ b/node_modules/sanity-plugin-media/src/components/DialogAssetEdit/Details.tsx
@@ -39,11 +39,11 @@ export default function Details({
         control={control}
         disabled={formUpdating}
         error={errors?.opt?.media?.tags?.message}
-        label="Tags"
+        label="タグ"
         name="opt.media.tags"
         onCreateTag={handleCreateTag}
         options={allTagOptions}
-        placeholder="Select or create..."
+        placeholder="選択または作成..."
         value={assetTagOptions}
       />
       {/* Filename */}
@@ -51,7 +51,7 @@ export default function Details({
         {...register('originalFilename')}
         disabled={formUpdating}
         error={errors?.originalFilename?.message}
-        label="Filename"
+        label="ファイル名"
         name="originalFilename"
         value={currentAsset?.originalFilename}
       />
@@ -60,7 +60,7 @@ export default function Details({
         {...register('title')}
         disabled={formUpdating}
         error={errors?.title?.message}
-        label="Title"
+        label="タイトル"
         name="title"
         value={currentAsset?.title}
       />
@@ -69,7 +69,7 @@ export default function Details({
         {...register('altText')}
         disabled={formUpdating}
         error={errors?.altText?.message}
-        label="Alt Text"
+        label="代替テキスト"
         name="altText"
         value={currentAsset?.altText}
       />
@@ -78,7 +78,7 @@ export default function Details({
         {...register('description')}
         disabled={formUpdating}
         error={errors?.description?.message}
-        label="Description"
+        label="説明"
         name="description"
         rows={5}
         value={currentAsset?.description}
@@ -88,7 +88,7 @@ export default function Details({
         <FormFieldInputText
           {...register('creditLine')}
           error={errors?.creditLine?.message}
-          label="Credit"
+          label="クレジット"
           name="creditLine"
           value={currentAsset?.creditLine}
           disabled={
diff --git a/node_modules/sanity-plugin-media/src/components/DialogAssetEdit/index.tsx b/node_modules/sanity-plugin-media/src/components/DialogAssetEdit/index.tsx
index b2e68b0..e77372f 100644
--- a/node_modules/sanity-plugin-media/src/components/DialogAssetEdit/index.tsx
+++ b/node_modules/sanity-plugin-media/src/components/DialogAssetEdit/index.tsx
@@ -222,7 +222,7 @@ const DialogAssetEdit = (props: Props) => {
           fontSize={1}
           mode="bleed"
           onClick={handleDelete}
-          text="Delete"
+          text="削除"
           tone="critical"
         />
 
@@ -258,7 +258,7 @@ const DialogAssetEdit = (props: Props) => {
     <Dialog
       animate
       footer={<Footer />}
-      header="Asset details"
+      header="アセットの詳細"
       id={id}
       onClose={handleClose}
       width={3}
@@ -281,7 +281,7 @@ const DialogAssetEdit = (props: Props) => {
                       aria-controls="details-panel"
                       disabled={formUpdating}
                       id="details-tab"
-                      label="Details"
+                      label="詳細"
                       onClick={() => setTabSection('details')}
                       selected={tabSection === 'details'}
                       size={2}
@@ -290,7 +290,7 @@ const DialogAssetEdit = (props: Props) => {
                       aria-controls="references-panel"
                       disabled={formUpdating}
                       id="references-tab"
-                      label={`References${
+                      label={`参照${
                         !isLoading && Array.isArray(uniqueReferringDocuments)
                           ? ` (${uniqueReferringDocuments.length})`
                           : ''
@@ -306,7 +306,7 @@ const DialogAssetEdit = (props: Props) => {
                     {/* Deleted notification */}
                     {!assetItem && (
                       <Card marginBottom={3} padding={3} radius={2} shadow={1} tone="critical">
-                        <Text size={1}>This file cannot be found – it may have been deleted.</Text>
+                        <Text size={1}>このファイルが見つかりません。削除された可能性があります。</Text>
                       </Card>
                     )}
 
diff --git a/node_modules/sanity-plugin-media/src/components/DialogConfirm/index.tsx b/node_modules/sanity-plugin-media/src/components/DialogConfirm/index.tsx
index cafeb2a..81b49e0 100644
--- a/node_modules/sanity-plugin-media/src/components/DialogConfirm/index.tsx
+++ b/node_modules/sanity-plugin-media/src/components/DialogConfirm/index.tsx
@@ -39,7 +39,7 @@ const DialogConfirm = (props: Props) => {
   const Footer = () => (
     <Box padding={3}>
       <Flex justify="space-between">
-        <Button fontSize={1} mode="bleed" onClick={handleClose} text="Cancel" />
+        <Button fontSize={1} mode="bleed" onClick={handleClose} text="キャンセル" />
         <Button
           fontSize={1}
           onClick={handleConfirm}
diff --git a/node_modules/sanity-plugin-media/src/components/DialogSearchFacets/index.tsx b/node_modules/sanity-plugin-media/src/components/DialogSearchFacets/index.tsx
index 646652b..eeb7ff2 100644
--- a/node_modules/sanity-plugin-media/src/components/DialogSearchFacets/index.tsx
+++ b/node_modules/sanity-plugin-media/src/components/DialogSearchFacets/index.tsx
@@ -27,7 +27,7 @@ const DialogSearchFacets = (props: Props) => {
   }, [])
 
   return (
-    <Dialog animate header="Filters" id={id} onClose={handleClose} width={1}>
+    <Dialog animate header="フィルター" id={id} onClose={handleClose} width={1}>
       <Box padding={3}>
         <SearchFacets layout="stack" />
         <SearchFacetsControl />
diff --git a/node_modules/sanity-plugin-media/src/components/DialogTagCreate/index.tsx b/node_modules/sanity-plugin-media/src/components/DialogTagCreate/index.tsx
index 84c37fd..c5c80e2 100644
--- a/node_modules/sanity-plugin-media/src/components/DialogTagCreate/index.tsx
+++ b/node_modules/sanity-plugin-media/src/components/DialogTagCreate/index.tsx
@@ -78,7 +78,7 @@ const DialogTagCreate = (props: Props) => {
   )
 
   return (
-    <Dialog animate footer={<Footer />} header="Create Tag" id={id} onClose={handleClose} width={1}>
+    <Dialog animate footer={<Footer />} header="タグを作成" id={id} onClose={handleClose} width={1}>
       {/* Form fields */}
       <Box as="form" padding={4} onSubmit={handleSubmit(onSubmit)}>
         {/* Hidden button to enable enter key submissions */}
@@ -89,7 +89,7 @@ const DialogTagCreate = (props: Props) => {
           {...register('name')}
           disabled={formUpdating}
           error={errors?.name?.message}
-          label="Name"
+          label="名前"
           name="name"
         />
       </Box>
diff --git a/node_modules/sanity-plugin-media/src/components/DialogTagEdit/index.tsx b/node_modules/sanity-plugin-media/src/components/DialogTagEdit/index.tsx
index df6a329..f9b4dcd 100644
--- a/node_modules/sanity-plugin-media/src/components/DialogTagEdit/index.tsx
+++ b/node_modules/sanity-plugin-media/src/components/DialogTagEdit/index.tsx
@@ -138,7 +138,7 @@ const DialogTagEdit = (props: Props) => {
           fontSize={1}
           mode="bleed"
           onClick={handleDelete}
-          text="Delete"
+          text="削除"
           tone="critical"
         />
 
@@ -158,13 +158,13 @@ const DialogTagEdit = (props: Props) => {
   }
 
   return (
-    <Dialog animate footer={<Footer />} header="Edit Tag" id={id} onClose={handleClose} width={1}>
+    <Dialog animate footer={<Footer />} header="タグを編集" id={id} onClose={handleClose} width={1}>
       {/* Form fields */}
       <Box as="form" padding={4} onSubmit={handleSubmit(onSubmit)}>
         {/* Deleted notification */}
         {!tagItem && (
           <Card marginBottom={3} padding={3} radius={2} shadow={1} tone="critical">
-            <Text size={1}>This tag cannot be found – it may have been deleted.</Text>
+            <Text size={1}>このタグが見つかりません。削除された可能性があります。</Text>
           </Card>
         )}
 
@@ -176,7 +176,7 @@ const DialogTagEdit = (props: Props) => {
           {...register('name')}
           disabled={formUpdating}
           error={errors?.name?.message}
-          label="Name"
+          label="名前"
           name="name"
         />
       </Box>
diff --git a/node_modules/sanity-plugin-media/src/components/DialogTags/index.tsx b/node_modules/sanity-plugin-media/src/components/DialogTags/index.tsx
index b72b689..f31d33f 100644
--- a/node_modules/sanity-plugin-media/src/components/DialogTags/index.tsx
+++ b/node_modules/sanity-plugin-media/src/components/DialogTags/index.tsx
@@ -26,7 +26,7 @@ const DialogTags = (props: Props) => {
   }, [])
 
   return (
-    <Dialog animate header="All Tags" id={id} onClose={handleClose} width={1}>
+    <Dialog animate header="すべてのタグ" id={id} onClose={handleClose} width={1}>
       <Box
         style={{
           height: '100%',
diff --git a/node_modules/sanity-plugin-media/src/components/DocumentList/index.tsx b/node_modules/sanity-plugin-media/src/components/DocumentList/index.tsx
index df15458..e72cab6 100644
--- a/node_modules/sanity-plugin-media/src/components/DocumentList/index.tsx
+++ b/node_modules/sanity-plugin-media/src/components/DocumentList/index.tsx
@@ -15,7 +15,7 @@ const DocumentList = ({documents, isLoading}: Props) => {
   if (isLoading) {
     return (
       <Text muted size={1}>
-        Loading...
+        読み込み中...
       </Text>
     )
   }
@@ -23,7 +23,7 @@ const DocumentList = ({documents, isLoading}: Props) => {
   if (documents.length === 0) {
     return (
       <Text muted size={1}>
-        No documents are referencing this asset
+        このアセットを参照しているドキュメントはありません
       </Text>
     )
   }
@@ -54,7 +54,7 @@ const ReferringDocument = (props: {doc: SanityDocument; schemaType?: SchemaType}
   ) : (
     <Box padding={2}>
       <Text size={1}>
-        A document of the unknown type <em>{doc._type}</em>
+        不明なタイプ「<em>{doc._type}</em>」のドキュメント
       </Text>
     </Box>
   )
diff --git a/node_modules/sanity-plugin-media/src/components/FormSubmitButton/index.tsx b/node_modules/sanity-plugin-media/src/components/FormSubmitButton/index.tsx
index 85c9d3a..3a572d9 100644
--- a/node_modules/sanity-plugin-media/src/components/FormSubmitButton/index.tsx
+++ b/node_modules/sanity-plugin-media/src/components/FormSubmitButton/index.tsx
@@ -17,16 +17,16 @@ const FormSubmitButton = (props: Props) => {
     if (lastUpdated) {
       content = (
         <>
-          Last updated
+          最終更新日
           <br /> {format(new Date(lastUpdated), 'PPp')}
         </>
       )
     } else {
-      content = 'No unpublished changes'
+      content = '未公開の変更はありません'
     }
   } else {
     content =
-      'There are validation errors that need to be fixed before this document can be published'
+      '公開する前に修正が必要なバリデーションエラーがあります'
   }
 
   return (
@@ -48,7 +48,7 @@ const FormSubmitButton = (props: Props) => {
           disabled={disabled}
           fontSize={1}
           onClick={onClick}
-          text="Save and close"
+          text="保存して閉じる"
           tone="primary"
         />
       </Box>
diff --git a/node_modules/sanity-plugin-media/src/components/Header/index.tsx b/node_modules/sanity-plugin-media/src/components/Header/index.tsx
index a780914..3994a25 100644
--- a/node_modules/sanity-plugin-media/src/components/Header/index.tsx
+++ b/node_modules/sanity-plugin-media/src/components/Header/index.tsx
@@ -1,7 +1,5 @@
 import {CloseIcon, Icon, UploadIcon} from '@sanity/icons'
 import {Box, Button, Flex, Inline, Text} from '@sanity/ui'
-import pluralize from 'pluralize'
-
 import {useAssetSourceActions} from '../../contexts/AssetSourceDispatchContext'
 import {useDropzoneActions} from '../../contexts/DropzoneDispatchContext'
 import useTypedSelector from '../../hooks/useTypedSelector'
@@ -31,7 +29,7 @@ const Header = (props: Props) => {
         <Box flex={1} marginX={3}>
           <Inline style={{whiteSpace: 'nowrap'}}>
             <Text textOverflow="ellipsis" weight="semibold">
-              <span>{onSelect ? `Insert ${assetTypes.join(' or ')}` : 'Browse Assets'}</span>
+              <span>{onSelect ? `${assetTypes.map(t => t === 'image' ? '画像' : 'ファイル').join('または')}を挿入` : 'アセットを閲覧'}</span>
             </Text>
 
             {selectedDocument && (
@@ -55,7 +53,7 @@ const Header = (props: Props) => {
               icon={UploadIcon}
               mode="bleed"
               onClick={open}
-              text={`Upload ${assetTypes.length === 1 ? pluralize(assetTypes[0]) : 'assets'}`}
+              text={`アップロード`}
             />
           )}
 
diff --git a/node_modules/sanity-plugin-media/src/components/PickedBar/index.tsx b/node_modules/sanity-plugin-media/src/components/PickedBar/index.tsx
index 7505b8b..0dd0e35 100644
--- a/node_modules/sanity-plugin-media/src/components/PickedBar/index.tsx
+++ b/node_modules/sanity-plugin-media/src/components/PickedBar/index.tsx
@@ -1,5 +1,4 @@
 import {Box, Button, Flex, Label} from '@sanity/ui'
-import pluralize from 'pluralize'
 import {useDispatch} from 'react-redux'
 import {useColorSchemeValue} from 'sanity'
 import {PANEL_HEIGHT} from '../../constants'
@@ -43,7 +42,7 @@ const PickedBar = () => {
       <Flex align="center" paddingX={3}>
         <Box paddingRight={2}>
           <Label size={0} style={{color: 'inherit'}}>
-            {assetsPicked.length} {pluralize('asset', assetsPicked.length)} selected
+            {assetsPicked.length}件のアセットを選択中
           </Label>
         </Box>
 
@@ -55,7 +54,7 @@ const PickedBar = () => {
           style={{background: 'none', boxShadow: 'none'}}
           tone="default"
         >
-          <Label size={0}>Deselect</Label>
+          <Label size={0}>選択解除</Label>
         </Button>
 
         {/* Delete button */}
@@ -66,7 +65,7 @@ const PickedBar = () => {
           style={{background: 'none', boxShadow: 'none'}}
           tone="critical"
         >
-          <Label size={0}>Delete</Label>
+          <Label size={0}>削除</Label>
         </Button>
       </Flex>
     </Flex>
diff --git a/node_modules/sanity-plugin-media/src/components/SearchFacetsControl/index.tsx b/node_modules/sanity-plugin-media/src/components/SearchFacetsControl/index.tsx
index c17ea25..1c80e79 100644
--- a/node_modules/sanity-plugin-media/src/components/SearchFacetsControl/index.tsx
+++ b/node_modules/sanity-plugin-media/src/components/SearchFacetsControl/index.tsx
@@ -111,7 +111,7 @@ const SearchFacetsControl = () => {
             icon={AddIcon}
             mode="bleed"
             space={2}
-            text="Add filter"
+            text="フィルターを追加"
             tone="primary"
           />
         }
@@ -129,7 +129,7 @@ const SearchFacetsControl = () => {
           fontSize={1}
           mode="bleed"
           onClick={() => dispatch(searchActions.facetsClear())}
-          text="Clear"
+          text="クリア"
         />
       )}
     </Flex>
diff --git a/node_modules/sanity-plugin-media/src/components/TableHeader/index.tsx b/node_modules/sanity-plugin-media/src/components/TableHeader/index.tsx
index 457b5bd..751977e 100644
--- a/node_modules/sanity-plugin-media/src/components/TableHeader/index.tsx
+++ b/node_modules/sanity-plugin-media/src/components/TableHeader/index.tsx
@@ -95,12 +95,12 @@ const TableHeader = () => {
       )}
 
       <TableHeaderItem />
-      <TableHeaderItem field="originalFilename" title="Filename" />
-      <TableHeaderItem title="Resolution" />
-      <TableHeaderItem field="mimeType" title="MIME type" />
-      <TableHeaderItem field="size" title="Size" />
-      <TableHeaderItem field="_updatedAt" title="Last updated" />
-      <TableHeaderItem title="References" />
+      <TableHeaderItem field="originalFilename" title="ファイル名" />
+      <TableHeaderItem title="解像度" />
+      <TableHeaderItem field="mimeType" title="MIMEタイプ" />
+      <TableHeaderItem field="size" title="サイズ" />
+      <TableHeaderItem field="_updatedAt" title="最終更新" />
+      <TableHeaderItem title="参照" />
       <TableHeaderItem />
     </Grid>
   )
diff --git a/node_modules/sanity-plugin-media/src/components/TagView/index.tsx b/node_modules/sanity-plugin-media/src/components/TagView/index.tsx
index 21fcf80..8c29c16 100644
--- a/node_modules/sanity-plugin-media/src/components/TagView/index.tsx
+++ b/node_modules/sanity-plugin-media/src/components/TagView/index.tsx
@@ -20,13 +20,13 @@ const TagView = () => {
       <TagViewHeader
         allowCreate
         light={hasPicked}
-        title={hasPicked ? 'Tags (in selection)' : 'Tags'}
+        title={hasPicked ? 'タグ（選択中）' : 'タグ'}
       />
 
       {fetchComplete && !hasTags && (
         <Box padding={3}>
           <Text muted size={1}>
-            <em>No tags</em>
+            <em>タグなし</em>
           </Text>
         </Box>
       )}
diff --git a/node_modules/sanity-plugin-media/src/components/TagViewHeader/index.tsx b/node_modules/sanity-plugin-media/src/components/TagViewHeader/index.tsx
index 95bd7c8..df780db 100644
--- a/node_modules/sanity-plugin-media/src/components/TagViewHeader/index.tsx
+++ b/node_modules/sanity-plugin-media/src/components/TagViewHeader/index.tsx
@@ -41,7 +41,7 @@ const TagViewHeader = ({allowCreate, light, title}: Props) => {
           <Label size={0}>{title}</Label>
           {tagsFetching && (
             <Label size={0} style={{opacity: 0.3}}>
-              Loading...
+              読み込み中...
             </Label>
           )}
         </Inline>
diff --git a/node_modules/sanity-plugin-media/src/components/TextInputSearch/index.tsx b/node_modules/sanity-plugin-media/src/components/TextInputSearch/index.tsx
index 03321d5..b322b3a 100644
--- a/node_modules/sanity-plugin-media/src/components/TextInputSearch/index.tsx
+++ b/node_modules/sanity-plugin-media/src/components/TextInputSearch/index.tsx
@@ -27,7 +27,7 @@ const TextInputSearch = () => {
         fontSize={1}
         icon={SearchIcon}
         onChange={handleChange}
-        placeholder="Search"
+        placeholder="検索"
         radius={2}
         value={searchQuery}
       />
diff --git a/node_modules/sanity-plugin-media/src/components/UploadDropzone/index.tsx b/node_modules/sanity-plugin-media/src/components/UploadDropzone/index.tsx
index 962deb2..6ad5f41 100644
--- a/node_modules/sanity-plugin-media/src/components/UploadDropzone/index.tsx
+++ b/node_modules/sanity-plugin-media/src/components/UploadDropzone/index.tsx
@@ -95,7 +95,7 @@ const UploadDropzone = (props: Props) => {
       dispatch(
         notificationsActions.add({
           status: 'error',
-          title: 'One or more files exceed the maximum upload size.'
+          title: 'アップロードサイズの上限を超えるファイルがあります。'
         })
       )
     }
@@ -126,7 +126,7 @@ const UploadDropzone = (props: Props) => {
       dispatch(
         notificationsActions.add({
           status: 'error',
-          title: `Unable to upload some items (folders and packages aren't supported)`
+          title: '一部のアイテムをアップロードできません（フォルダとパッケージは非対応）'
         })
       )
     }
@@ -157,7 +157,7 @@ const UploadDropzone = (props: Props) => {
           <DragActiveContainer>
             <Flex direction="column" justify="center" style={{color: white.hex}}>
               <Text size={3} style={{color: 'inherit'}}>
-                Drop files to upload
+                ファイルをドロップしてアップロード
               </Text>
             </Flex>
           </DragActiveContainer>
diff --git a/node_modules/sanity-plugin-media/src/config/orders.ts b/node_modules/sanity-plugin-media/src/config/orders.ts
index 5e59499..f592261 100644
--- a/node_modules/sanity-plugin-media/src/config/orders.ts
+++ b/node_modules/sanity-plugin-media/src/config/orders.ts
@@ -2,24 +2,24 @@ import type {OrderDirection} from '../types'
 
 const ORDER_DICTIONARY: Record<string, {asc: string; desc: string}> = {
   _createdAt: {
-    asc: 'Last created: Oldest first',
-    desc: 'Last created: Newest first'
+    asc: '作成日：古い順',
+    desc: '作成日：新しい順'
   },
   _updatedAt: {
-    asc: 'Last updated: Oldest first',
-    desc: 'Last updated: Newest first'
+    asc: '更新日：古い順',
+    desc: '更新日：新しい順'
   },
   mimeType: {
-    asc: 'MIME type: A to Z',
-    desc: 'MIME type: Z to A'
+    asc: 'MIMEタイプ：A→Z',
+    desc: 'MIMEタイプ：Z→A'
   },
   originalFilename: {
-    asc: 'File name: A to Z',
-    desc: 'File name: Z to A'
+    asc: 'ファイル名：A→Z',
+    desc: 'ファイル名：Z→A'
   },
   size: {
-    asc: 'File size: Smallest first',
-    desc: 'File size: Largest first'
+    asc: 'ファイルサイズ：小さい順',
+    desc: 'ファイルサイズ：大きい順'
   }
 }
 
diff --git a/node_modules/sanity-plugin-media/src/config/searchFacets.ts b/node_modules/sanity-plugin-media/src/config/searchFacets.ts
index 5f1f699..06e4459 100644
--- a/node_modules/sanity-plugin-media/src/config/searchFacets.ts
+++ b/node_modules/sanity-plugin-media/src/config/searchFacets.ts
@@ -15,7 +15,7 @@ export const inputs: Record<SearchFacetName, SearchFacetInputProps> = {
     name: 'altText',
     operatorType: 'empty',
     operatorTypes: ['empty', 'notEmpty', null, 'includes', 'doesNotInclude'],
-    title: 'Alt text',
+    title: '代替テキスト',
     type: 'string',
     value: ''
   },
@@ -25,7 +25,7 @@ export const inputs: Record<SearchFacetName, SearchFacetInputProps> = {
     name: 'creditLine',
     operatorType: 'empty',
     operatorTypes: ['empty', 'notEmpty', null, 'includes', 'doesNotInclude'],
-    title: 'Credit',
+    title: 'クレジット',
     type: 'string',
     value: ''
   },
@@ -35,7 +35,7 @@ export const inputs: Record<SearchFacetName, SearchFacetInputProps> = {
     name: 'description',
     operatorType: 'empty',
     operatorTypes: ['empty', 'notEmpty', null, 'includes', 'doesNotInclude'],
-    title: 'Description',
+    title: '説明',
     type: 'string',
     value: ''
   },
@@ -45,7 +45,7 @@ export const inputs: Record<SearchFacetName, SearchFacetInputProps> = {
     name: 'filename',
     operatorType: 'includes',
     operatorTypes: ['includes', 'doesNotInclude'],
-    title: 'File name',
+    title: 'ファイル名',
     type: 'string',
     value: ''
   },
@@ -62,7 +62,7 @@ export const inputs: Record<SearchFacetName, SearchFacetInputProps> = {
       null,
       'equalTo'
     ],
-    title: 'Height',
+    title: '高さ',
     type: 'number',
     value: 400
   },
@@ -73,17 +73,17 @@ export const inputs: Record<SearchFacetName, SearchFacetInputProps> = {
     options: [
       {
         name: 'true',
-        title: 'True',
+        title: 'はい',
         value: groq`_id in $documentAssetIds`
       },
       {
         name: 'false',
-        title: 'False',
+        title: 'いいえ',
         value: groq`!(_id in $documentAssetIds)`
       }
     ],
     selectOnly: true,
-    title: 'In use in current document',
+    title: '現在のドキュメントで使用中',
     type: 'select',
     value: 'true'
   },
@@ -94,16 +94,16 @@ export const inputs: Record<SearchFacetName, SearchFacetInputProps> = {
     options: [
       {
         name: 'true',
-        title: 'True',
+        title: 'はい',
         value: groq`count(*[references(^._id)]) > 0`
       },
       {
         name: 'false',
-        title: 'False',
+        title: 'いいえ',
         value: groq`count(*[references(^._id)]) == 0`
       }
     ],
-    title: 'In use',
+    title: '使用中',
     type: 'select',
     value: 'true'
   },
@@ -115,16 +115,16 @@ export const inputs: Record<SearchFacetName, SearchFacetInputProps> = {
     options: [
       {
         name: 'true',
-        title: 'True',
+        title: 'はい',
         value: `false`
       },
       {
         name: 'false',
-        title: 'False',
+        title: 'いいえ',
         value: `true`
       }
     ],
-    title: 'Has transparency',
+    title: '透明度あり',
     type: 'select',
     value: 'true'
   },
@@ -136,21 +136,21 @@ export const inputs: Record<SearchFacetName, SearchFacetInputProps> = {
     options: [
       {
         name: 'portrait',
-        title: 'Portrait',
+        title: '縦向き',
         value: 'metadata.dimensions.aspectRatio < 1'
       },
       {
         name: 'landscape',
-        title: 'Landscape',
+        title: '横向き',
         value: 'metadata.dimensions.aspectRatio > 1'
       },
       {
         name: 'square',
-        title: 'Square',
+        title: '正方形',
         value: 'metadata.dimensions.aspectRatio == 1'
       }
     ],
-    title: 'Orientation',
+    title: '向き',
     type: 'select',
     value: 'portrait'
   },
@@ -180,7 +180,7 @@ export const inputs: Record<SearchFacetName, SearchFacetInputProps> = {
       null,
       'equalTo'
     ],
-    title: 'File size',
+    title: 'ファイルサイズ',
     type: 'number',
     value: 0
   },
@@ -190,7 +190,7 @@ export const inputs: Record<SearchFacetName, SearchFacetInputProps> = {
     name: 'tag',
     operatorType: 'references',
     operatorTypes: ['references', 'doesNotReference', null, 'empty', 'notEmpty'],
-    title: 'Tags',
+    title: 'タグ',
     type: 'searchable'
   },
   title: {
@@ -199,7 +199,7 @@ export const inputs: Record<SearchFacetName, SearchFacetInputProps> = {
     name: 'title',
     operatorType: 'empty',
     operatorTypes: ['empty', 'notEmpty', null, 'includes', 'doesNotInclude'],
-    title: 'Title',
+    title: 'タイトル',
     type: 'string',
     value: ''
   },
@@ -211,17 +211,17 @@ export const inputs: Record<SearchFacetName, SearchFacetInputProps> = {
     options: [
       {
         name: 'image',
-        title: 'Image',
+        title: '画像',
         value: 'mimeType match "image*"'
       },
       {
         name: 'video',
-        title: 'Video',
+        title: '動画',
         value: 'mimeType match "video*"'
       },
       {
         name: 'audio',
-        title: 'Audio',
+        title: 'オーディオ',
         value: 'mimeType match "audio*"'
       },
       {
@@ -230,7 +230,7 @@ export const inputs: Record<SearchFacetName, SearchFacetInputProps> = {
         value: 'mimeType == "application/pdf"'
       }
     ],
-    title: 'File type',
+    title: 'ファイルタイプ',
     type: 'select',
     value: 'image'
   },
@@ -247,7 +247,7 @@ export const inputs: Record<SearchFacetName, SearchFacetInputProps> = {
       null,
       'equalTo'
     ],
-    title: 'Width',
+    title: '幅',
     type: 'number',
     value: 400
   }
@@ -256,56 +256,56 @@ export const inputs: Record<SearchFacetName, SearchFacetInputProps> = {
 export const operators: SearchFacetOperators = {
   doesNotInclude: {
     fn: (value, field) => (value ? `!(${field} match '*${value}*')` : undefined),
-    label: 'does not include'
+    label: 'を含まない'
   },
   doesNotReference: {
     fn: (value, _field) => (value ? `!references('${value}')` : undefined),
-    label: 'does not include'
+    label: 'を含まない'
   },
   empty: {
     fn: (_value, field) => `!defined(${field})`,
     hideInput: true,
-    label: 'is empty'
+    label: '空である'
   },
   equalTo: {
     fn: (value, field) => (value ? `${field} == ${value}` : undefined),
-    label: 'is equal to'
+    label: 'と等しい'
   },
   greaterThan: {
     fn: (value, field) => (value ? `${field} > ${value}` : undefined),
-    label: 'is greater than'
+    label: 'より大きい'
   },
   greaterThanOrEqualTo: {
     fn: (value, field) => (value ? `${field} >= ${value}` : undefined),
-    label: 'is greater than or equal to'
+    label: '以上'
   },
   includes: {
     fn: (value, field) => (value ? `${field} match '*${value}*'` : undefined),
-    label: 'includes'
+    label: 'を含む'
   },
   is: {
     fn: (value, _field) => `${value}`,
-    label: 'is'
+    label: 'である'
   },
   isNot: {
     fn: (value, _field) => `!(${value})`,
-    label: 'is not'
+    label: 'ではない'
   },
   lessThan: {
     fn: (value, field) => (value ? `${field} < ${value}` : undefined),
-    label: 'is less than'
+    label: 'より小さい'
   },
   lessThanOrEqualTo: {
     fn: (value, field) => (value ? `${field} <= ${value}` : undefined),
-    label: 'is less than or equal to'
+    label: '以下'
   },
   notEmpty: {
     fn: (_value, field) => `defined(${field})`,
     hideInput: true,
-    label: 'is not empty'
+    label: '空でない'
   },
   references: {
     fn: (value, _field) => (value ? `references('${value}')` : undefined),
-    label: 'includes'
+    label: 'を含む'
   }
 }
diff --git a/node_modules/sanity-plugin-media/src/formSchema/index.ts b/node_modules/sanity-plugin-media/src/formSchema/index.ts
index 92401a2..a8f5527 100644
--- a/node_modules/sanity-plugin-media/src/formSchema/index.ts
+++ b/node_modules/sanity-plugin-media/src/formSchema/index.ts
@@ -1,8 +1,8 @@
 import * as z from 'zod'
 
 export const tagOptionSchema = z.object({
-  label: z.string().trim().min(1, {message: 'Label cannot be empty'}),
-  value: z.string().trim().min(1, {message: 'Value cannot be empty'})
+  label: z.string().trim().min(1, {message: 'ラベルを入力してください'}),
+  value: z.string().trim().min(1, {message: '値を入力してください'})
 })
 
 export const assetFormSchema = z.object({
@@ -14,10 +14,10 @@ export const assetFormSchema = z.object({
       tags: z.array(tagOptionSchema).nullable()
     })
   }),
-  originalFilename: z.string().trim().min(1, {message: 'Filename cannot be empty'}),
+  originalFilename: z.string().trim().min(1, {message: 'ファイル名を入力してください'}),
   title: z.string().trim().optional()
 })
 
 export const tagFormSchema = z.object({
-  name: z.string().min(1, {message: 'Name cannot be empty'})
+  name: z.string().min(1, {message: '名前を入力してください'})
 })
diff --git a/node_modules/sanity-plugin-media/src/modules/dialog/index.ts b/node_modules/sanity-plugin-media/src/modules/dialog/index.ts
index 4a6ef43..6d03da6 100644
--- a/node_modules/sanity-plugin-media/src/modules/dialog/index.ts
+++ b/node_modules/sanity-plugin-media/src/modules/dialog/index.ts
@@ -1,6 +1,5 @@
 import {createSlice, type PayloadAction} from '@reduxjs/toolkit'
 import type {AssetItem, Dialog, MyEpic, Tag} from '../../types'
-import pluralize from 'pluralize'
 import {ofType} from 'redux-observable'
 import {EMPTY, of} from 'rxjs'
 import {filter, mergeMap} from 'rxjs/operators'
@@ -79,18 +78,16 @@ const dialogSlice = createSlice({
     ) {
       const {assetsPicked, closeDialogId, tag} = action.payload
 
-      const suffix = `${assetsPicked.length} ${pluralize('asset', assetsPicked.length)}`
-
       state.items.push({
         closeDialogId,
         confirmCallbackAction: ASSETS_ACTIONS.tagsAddRequest({
           assets: assetsPicked,
           tag
         }),
-        confirmText: `Yes, add tag to ${suffix}`,
-        title: `Add tag ${tag.name.current} to ${suffix}?`,
+        confirmText: `はい、${assetsPicked.length}件のアセットにタグを追加`,
+        title: `${assetsPicked.length}件のアセットにタグ「${tag.name.current}」を追加しますか？`,
         id: 'confirm',
-        headerTitle: 'Confirm tag addition',
+        headerTitle: 'タグ追加の確認',
         tone: 'primary',
         type: 'confirm'
       })
@@ -105,15 +102,13 @@ const dialogSlice = createSlice({
     ) {
       const {assetsPicked, closeDialogId, tag} = action.payload
 
-      const suffix = `${assetsPicked.length} ${pluralize('asset', assetsPicked.length)}`
-
       state.items.push({
         closeDialogId,
         confirmCallbackAction: ASSETS_ACTIONS.tagsRemoveRequest({assets: assetsPicked, tag}),
-        confirmText: `Yes, remove tag from ${suffix}`,
-        headerTitle: 'Confirm tag removal',
+        confirmText: `はい、${assetsPicked.length}件のアセットからタグを削除`,
+        headerTitle: 'タグ削除の確認',
         id: 'confirm',
-        title: `Remove tag ${tag.name.current} from ${suffix}?`,
+        title: `${assetsPicked.length}件のアセットからタグ「${tag.name.current}」を削除しますか？`,
         tone: 'critical',
         type: 'confirm'
       })
@@ -124,18 +119,16 @@ const dialogSlice = createSlice({
     ) {
       const {assets, closeDialogId} = action.payload
 
-      const suffix = `${assets.length} ${pluralize('asset', assets.length)}`
-
       state.items.push({
         closeDialogId,
         confirmCallbackAction: assetsActions.deleteRequest({
           assets: assets.map(assetItem => assetItem.asset)
         }),
-        confirmText: `Yes, delete ${suffix}`,
-        description: 'This operation cannot be reversed. Are you sure you want to continue?',
-        title: `Permanently delete ${suffix}?`,
+        confirmText: `はい、${assets.length}件のアセットを削除`,
+        description: 'この操作は元に戻せません。続行しますか？',
+        title: `${assets.length}件のアセットを完全に削除しますか？`,
         id: 'confirm',
-        headerTitle: 'Confirm deletion',
+        headerTitle: '削除の確認',
         tone: 'critical',
         type: 'confirm'
       })
@@ -143,16 +136,14 @@ const dialogSlice = createSlice({
     showConfirmDeleteTag(state, action: PayloadAction<{closeDialogId?: string; tag: Tag}>) {
       const {closeDialogId, tag} = action.payload
 
-      const suffix = 'tag'
-
       state.items.push({
         closeDialogId,
         confirmCallbackAction: tagsActions.deleteRequest({tag}),
-        confirmText: `Yes, delete ${suffix}`,
-        description: 'This operation cannot be reversed. Are you sure you want to continue?',
-        title: `Permanently delete ${suffix}?`,
+        confirmText: 'はい、タグを削除',
+        description: 'この操作は元に戻せません。続行しますか？',
+        title: 'タグを完全に削除しますか？',
         id: 'confirm',
-        headerTitle: 'Confirm deletion',
+        headerTitle: '削除の確認',
         tone: 'critical',
         type: 'confirm'
       })
diff --git a/node_modules/sanity-plugin-media/src/modules/notifications/index.ts b/node_modules/sanity-plugin-media/src/modules/notifications/index.ts
index 6f7e7c0..70eab95 100644
--- a/node_modules/sanity-plugin-media/src/modules/notifications/index.ts
+++ b/node_modules/sanity-plugin-media/src/modules/notifications/index.ts
@@ -1,6 +1,5 @@
 import {type PayloadAction, createSlice} from '@reduxjs/toolkit'
 import type {ImageAsset, MyEpic} from '../../types'
-import pluralize from 'pluralize'
 import {ofType} from 'redux-observable'
 import {of} from 'rxjs'
 import {bufferTime, filter, mergeMap} from 'rxjs/operators'
@@ -49,7 +48,7 @@ export const notificationsAssetsDeleteCompleteEpic: MyEpic = action$ =>
       return of(
         notificationsSlice.actions.add({
           status: 'info',
-          title: `${deletedCount} ${pluralize('asset', deletedCount)} deleted`
+          title: `${deletedCount}件のアセットを削除しました`
         })
       )
     })
@@ -64,10 +63,7 @@ export const notificationsAssetsDeleteErrorEpic: MyEpic = action$ =>
       return of(
         notificationsSlice.actions.add({
           status: 'error',
-          title: `Unable to delete ${count} ${pluralize(
-            'asset',
-            count
-          )}. Please review any asset errors and try again.`
+          title: `${count}件のアセットを削除できませんでした。エラーを確認してもう一度お試しください。`
         })
       )
     })
@@ -83,7 +79,7 @@ export const notificationsAssetsUploadCompleteEpic: MyEpic = action$ =>
       return of(
         notificationsSlice.actions.add({
           status: 'info',
-          title: `Uploaded ${count} ${pluralize('asset', count)}`
+          title: `${count}件のアセットをアップロードしました`
         })
       )
     })
@@ -97,7 +93,7 @@ export const notificationsAssetsTagsAddCompleteEpic: MyEpic = action$ =>
       return of(
         notificationsSlice.actions.add({
           status: 'info',
-          title: `Tag added to ${count} ${pluralize('asset', count)}`
+          title: `${count}件のアセットにタグを追加しました`
         })
       )
     })
@@ -111,7 +107,7 @@ export const notificationsAssetsTagsRemoveCompleteEpic: MyEpic = action$ =>
       return of(
         notificationsSlice.actions.add({
           status: 'info',
-          title: `Tag removed from ${count} ${pluralize('asset', count)}`
+          title: `${count}件のアセットからタグを削除しました`
         })
       )
     })
@@ -127,7 +123,7 @@ export const notificationsAssetsUpdateCompleteEpic: MyEpic = action$ =>
       return of(
         notificationsSlice.actions.add({
           status: 'info',
-          title: `${updatedCount} ${pluralize('asset', updatedCount)} updated`
+          title: `${updatedCount}件のアセットを更新しました`
         })
       )
     })
@@ -149,7 +145,7 @@ export const notificationsGenericErrorEpic: MyEpic = action$ =>
       return of(
         notificationsSlice.actions.add({
           status: 'error',
-          title: `An error occured: ${error.message}`
+          title: `エラーが発生しました: ${error.message}`
         })
       )
     })
@@ -158,19 +154,19 @@ export const notificationsGenericErrorEpic: MyEpic = action$ =>
 export const notificationsTagCreateCompleteEpic: MyEpic = action$ =>
   action$.pipe(
     filter(tagsActions.createComplete.match),
-    mergeMap(() => of(notificationsSlice.actions.add({status: 'info', title: `Tag created`})))
+    mergeMap(() => of(notificationsSlice.actions.add({status: 'info', title: 'タグを作成しました'})))
   )
 
 export const notificationsTagDeleteCompleteEpic: MyEpic = action$ =>
   action$.pipe(
     filter(tagsActions.deleteComplete.match),
-    mergeMap(() => of(notificationsSlice.actions.add({status: 'info', title: `Tag deleted`})))
+    mergeMap(() => of(notificationsSlice.actions.add({status: 'info', title: 'タグを削除しました'})))
   )
 
 export const notificationsTagUpdateCompleteEpic: MyEpic = action$ =>
   action$.pipe(
     filter(tagsActions.updateComplete.match),
-    mergeMap(() => of(notificationsSlice.actions.add({status: 'info', title: `Tag updated`})))
+    mergeMap(() => of(notificationsSlice.actions.add({status: 'info', title: 'タグを更新しました'})))
   )
 
 export const notificationsActions = {...notificationsSlice.actions}
diff --git a/node_modules/sanity-plugin-media/src/plugin.tsx b/node_modules/sanity-plugin-media/src/plugin.tsx
index b3c7956..5382693 100644
--- a/node_modules/sanity-plugin-media/src/plugin.tsx
+++ b/node_modules/sanity-plugin-media/src/plugin.tsx
@@ -9,7 +9,7 @@ import {ToolOptionsProvider} from './contexts/ToolOptionsContext'
 const plugin = {
   icon: ImageIcon,
   name: 'media',
-  title: 'Media'
+  title: 'メディア'
 }
 
 export const mediaAssetSource = {
diff --git a/node_modules/sanity-plugin-media/src/schemas/tag.ts b/node_modules/sanity-plugin-media/src/schemas/tag.ts
index 281c732..5353dab 100644
--- a/node_modules/sanity-plugin-media/src/schemas/tag.ts
+++ b/node_modules/sanity-plugin-media/src/schemas/tag.ts
@@ -2,13 +2,13 @@ import {TAG_DOCUMENT_NAME} from '../constants'
 import TagIcon from '../components/TagIcon'
 
 export default {
-  title: 'Media Tag',
+  title: 'メディアタグ',
   icon: TagIcon,
   name: TAG_DOCUMENT_NAME,
   type: 'document',
   fields: [
     {
-      title: 'Name',
+      title: '名前',
       name: 'name',
       type: 'slug'
     }
